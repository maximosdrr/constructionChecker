import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:constructionChecker/app/checkList/check_list_page.dart';

void main() {
  testWidgets('CheckListPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CheckListPage(title: 'CheckList')));
    //  final titleFinder = find.text('CheckList');
    //  expect(titleFinder, findsOneWidget);
  });
}
