import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:constructionChecker/app/checkList/check_list_controller.dart';
import 'package:constructionChecker/app/checkList/check_list_module.dart';

void main() {
  initModule(CheckListModule());
  // CheckListController checklist;
  //
  setUp(() {
    //     checklist = CheckListModule.to.get<CheckListController>();
  });

  group('CheckListController Test', () {
    //   test("First Test", () {
    //     expect(checklist, isInstanceOf<CheckListController>());
    //   });

    //   test("Set Value", () {
    //     expect(checklist.value, equals(0));
    //     checklist.increment();
    //     expect(checklist.value, equals(1));
    //   });
  });
}
