import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:constructionChecker/app/modules/cornerStone/corner_stone_page.dart';

void main() {
  testWidgets('CornerStonePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CornerStonePage(title: 'CornerStone')));
    //  final titleFinder = find.text('CornerStone');
    //  expect(titleFinder, findsOneWidget);
  });
}
