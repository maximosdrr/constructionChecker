import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:constructionChecker/app/modules/cornerStone/corner_stone_controller.dart';
import 'package:constructionChecker/app/modules/cornerStone/corner_stone_module.dart';

void main() {
  initModule(CornerStoneModule());
  // CornerStoneController cornerstone;
  //
  setUp(() {
    //     cornerstone = CornerStoneModule.to.get<CornerStoneController>();
  });

  group('CornerStoneController Test', () {
    //   test("First Test", () {
    //     expect(cornerstone, isInstanceOf<CornerStoneController>());
    //   });

    //   test("Set Value", () {
    //     expect(cornerstone.value, equals(0));
    //     cornerstone.increment();
    //     expect(cornerstone.value, equals(1));
    //   });
  });
}
