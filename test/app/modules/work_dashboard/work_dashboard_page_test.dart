import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:constructionChecker/app/modules/work_dashboard/work_dashboard_page.dart';

void main() {
  testWidgets('WorkDashboardPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(WorkDashboardPage(title: 'WorkDashboard')));
    //  final titleFinder = find.text('WorkDashboard');
    //  expect(titleFinder, findsOneWidget);
  });
}
