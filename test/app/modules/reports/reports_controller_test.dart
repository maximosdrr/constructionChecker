import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:constructionChecker/app/modules/reports/reports_controller.dart';
import 'package:constructionChecker/app/modules/reports/reports_module.dart';

void main() {
  initModule(ReportsModule());
  // ReportsController reports;
  //
  setUp(() {
    //     reports = ReportsModule.to.get<ReportsController>();
  });

  group('ReportsController Test', () {
    //   test("First Test", () {
    //     expect(reports, isInstanceOf<ReportsController>());
    //   });

    //   test("Set Value", () {
    //     expect(reports.value, equals(0));
    //     reports.increment();
    //     expect(reports.value, equals(1));
    //   });
  });
}
