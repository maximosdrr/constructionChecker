import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:constructionChecker/app/modules/corner_stone_history/corner_stone_history_page.dart';

void main() {
  testWidgets('CornerStoneHistoryPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CornerStoneHistoryPage(title: 'CornerStoneHistory')));
    //  final titleFinder = find.text('CornerStoneHistory');
    //  expect(titleFinder, findsOneWidget);
  });
}
