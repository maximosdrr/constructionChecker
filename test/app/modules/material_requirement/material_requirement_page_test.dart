import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:constructionChecker/app/modules/material_requirement/material_requirement_page.dart';

void main() {
  testWidgets('MaterialRequirementPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MaterialRequirementPage(title: 'MaterialRequirement')));
    //  final titleFinder = find.text('MaterialRequirement');
    //  expect(titleFinder, findsOneWidget);
  });
}
